[x] Split the topology in two structs.
[ ] Implement multiple execution loop, just one thread.
[ ] Multithread this shit out 
[ ] Refactor all the `state.users.items(.session_gen)[current_uid]` into a `session`
[ ] Check the information the events need, maybe passing the whole graph is just more optimal
